#ifndef WORLDDEFS_H
#define WORLDDEFS_H

const float STRAFE_SPEED = 0.2f;
const float ADVANCE_SPEED = 0.25f;

typedef enum{C_ENEMY, C_DOOR, C_LOCK, C_LIGHT, C_WALL, C_FLOOR, C_DEFAULT} WALL_TYPE;

#endif